<%= form_tag(search_path, :method => "get") do %>
<div class="input-group">
        <%= search_field_tag :search, params[:search], placeholder: "Type ", class: "form-control" %>
        <div class="input-group-btn">
            <%= button_tag "Search for Notes", :class => 'btn btn-info glyphicon-search', :name
            => nil%>
        </div>
            <div class= "input-group-btn">
                <%= button_tag "Search for Assignments", :class => 'btn btn-info glyphicon-search', :name
                => nil%>
            </div>
</div>
<% end %> 